Date: 2026-04-30

Changed / amended / added / deleted

- Amended index.html mobile layout for the bottom station list.
- Changed the mobile station list from a wrapping grid into a single horizontal row.
- Added native horizontal swipe support on mobile by using x-axis overflow scrolling, scroll snapping, and touch pan behavior.
- Kept each mobile station item to one line with station number, station name, and cumulative distance.
- Added this dated documentation file.
- Deleted nothing.

Decisions made

- Kept the desktop station list as the existing multi-column grid.
- Applied the one-line swipeable station tray only at small screen widths, currently max-width: 640px.
- Used native browser scrolling instead of custom gesture JavaScript so mobile swipe behavior stays reliable and lightweight.
- Kept the existing click behavior: tapping a station still centers the map and opens that station popup.

What we have done

- The bottom station tray on mobile now displays station info in a single row.
- Swiping left moves forward through later stations.
- Swiping right moves back to previous stations.
- Station pills are scroll-snapped so the tray lands cleanly while browsing the list.

Additional update: GPS and station focus

What changed

- Added a GPS map control that starts and stops browser location tracking.
- Added a user-location marker and accuracy circle that update when GPS fixes arrive.
- Added automatic bottom station-button centering when a stamp marker popup opens.
- Added automatic bottom station-button centering when GPS is within 10 meters of a stamp station.
- Added an active station-button state so the focused station is visually clear.

Why

- The stamp rally map now works better while walking the route because the user can see their current GPS position.
- The bottom station banner now stays synchronized with map interactions, reducing manual scrolling after selecting a stamp spot.
- The 10-meter proximity behavior makes the station banner react automatically when the user arrives at a stamp station.
